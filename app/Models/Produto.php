<?php
declare(strict_types=1);
namespace App\Models;

/**
 * Model Produto (esqueleto).
 * Depois vamos validar dados e acessar o banco via PDO.
 */
class Produto {
    // TODO: validate(), all(), find(), create(), update(), delete()
}
